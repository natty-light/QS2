Add support for float literals

add support for defining functions like:
func add(a, b) {
    a + b
}

add support for % and integer division

instanceof operator?

ensure that line number tracking for booleans work, since we set the line number in the creation function